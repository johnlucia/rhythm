<div id="page-wrap" class="two-column">
  <div class="main-content">
    <%= raw(@page.content_for(:body)) %>
  </div>

  <div class="side-content">
    <%= raw(@page.content_for(:side_body)) %>
  </div>

  <div class="googus"></div>
</div>